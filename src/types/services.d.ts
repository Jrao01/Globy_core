// Tipos compartidos para servicios, por ejemplo payloads, respuestas o resultados estándar.
// Añade aquí las definiciones de tipos que deben ser reutilizadas en servicios.

export type ServiceResult<T> = Promise<T>;
